pipeline {
    agent any
    //{label 'Revathi'}
 
    stages {

      stage('clean')
            {
                steps
                 { 
                    sh 'mvn clean install'
                 }
            }
}
}
