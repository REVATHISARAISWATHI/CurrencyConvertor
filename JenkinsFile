pipeline {
libraries{
lib 'shlib'
}
    agent any
    tools{
    maven 'Maven'
    }
 
    stages {
      stage('Start')
            {
                steps
                 { 
                    sendNotifications 'STARTED'
                 }
            }
  
      stage('clean')
            {
                steps
                 { 
                    sh 'mvn clean install'
                 }
            }
}
post{
always{
sendNotifications currentBuild.result
}
}
}
