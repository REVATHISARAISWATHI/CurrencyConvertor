pipeline {
libraries{
lib 'shlib'
}
tool
{
Maven 'Maven'
}

    agent any
 
    stages {
      stage('Start')
            {
                steps
                 { 
                    sendNotifications 'STARTED'
                 }
            }
  
      stage('clean')
            {
                steps
                 { 
                    sh 'mvn clean install'
                 }
            }
}
post{
always{
sendNotifications currentBuild.result
}
}
}
