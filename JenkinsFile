pipeline {
agent any
   // agent {label 'Revathi'}
 
    stages {

      stage('clean')
            {
                steps
                 { 
                    sh 'mvn clean install'
                 }
            }
}
}
