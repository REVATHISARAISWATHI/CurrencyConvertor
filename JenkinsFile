pipeline {
    agent {lable 'Revathi'}
 
    stages {

      stage('clean')
            {
                steps
                 { 
                    sh 'mvn clean install'
                 }
            }
}
}
